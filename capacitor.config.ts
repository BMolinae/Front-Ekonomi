const config = { 
  appId: 'com.ekonomi.finanzas',
  appName: 'ekonomi-frontend',
  webDir: 'www',
  bundledWebRuntime: false,
  plugins: {
    Keyboard: {
      resize: 'ionic'  // <- clave para que la app se ajuste con teclado
    }
  }
};

export default config;
